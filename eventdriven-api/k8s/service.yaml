apiVersion: v1
kind: Service
metadata:
  name: eventdriven-api-service
spec:
  selector:
    app: eventdriven-api
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
